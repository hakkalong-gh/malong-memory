# 2026-03-22 Python 版本检查复盘

## 事件回顾

**时间**: 2026-03-22 10:30-10:35

**触发**: 安装 widemem-ai 失败

**经过**:
| 时间 | 事件 | 我的行为 | 问题 |
|------|------|---------|------|
| 10:30 | pip install widemem-ai 失败 | 检查 `python3 --version` | ❌ 单一检查 |
| 10:30 | 显示 Python 3.9.5 | 汇报"需要升级 Python" | ❌ 错误结论 |
| 10:35 | DL 提醒"昨天好像装过" | 检查 `python3.11 --version` | ✅ 找到真相 |
| 10:35 | 显示 Python 3.11.15 | 承认错误 | ✅ 但本应避免 |

---

## 根因分析（3 层）

### 第 1 层：技术原因

**检查方法单一**
```bash
❌ 只检查：python3 --version → 3.9.5
✅ 应该检查：
   - python3.11 --version → 3.11.15
   - uv python list → 已安装版本
   - which python3.11 → 路径
   - ls /usr/bin/python* → 所有版本
```

**惯性思维**：
- 默认 `python3` = 系统唯一 Python
- 没想到可能有多个版本并存
- 没想到 uv 管理的独立环境

---

### 第 2 层：流程原因

**没有搜索历史记录**

根据 `AGENTS.md` 和 `MEMORY.md` 规定：
> 重要操作前先用 `memory_search` 搜索历史记录

但我：
- ❌ 没有搜索"Python 升级"
- ❌ 没有搜索"2026-03-21"
- ❌ 没有检查昨天的对话记录

**如果搜索了**：
```
memory_search("Python 升级 2026-03-21")
→ 找到昨天记录：Python 3.11.15 已安装
→ 直接跳过版本检查，尝试安装 widemem-ai
```

---

### 第 3 层：认知原因

**"失败后没有多方案验证"**

这正是我们刚才总结的"发呆 17 分钟"的根因**重复出现**：

| 今天的问题 | 昨天的教训 |
|-----------|-----------|
| pip 失败→说装不了 | 抖音抓取失败→说技术限制 |
| 只检查 python3 | 只尝试抓取抖音 |
| 没有检查 python3.11 | 没有尝试 Serper 搜索 |
| 等 DL 提醒才深挖 | 等 DL 问才汇报 |

**模式重复了！**

---

## 深层问题

### 问题 1：记忆搜索机制失效

**规定**：每次会话启动读取 memory 文件  
**实际**：读了，但**没有主动搜索关键信息**

**改进**：
```python
# 会话启动流程（再升级）

1. 读取 TODO.md
2. 读取 MEMORY.md
3. ✅ 新增：memory_search 搜索当前任务相关历史
   - 如任务涉及"Python"→搜索"Python 升级"
   - 如任务涉及"安装"→搜索历史安装记录
   - 如任务涉及"配置"→搜索历史配置记录
4. 汇报进度（包括历史检查结果）
```

---

### 问题 2：失败后没有"多源验证"

**昨天教训**：抖音抓取失败 → 应该立即尝试 Serper  
**今天重复**：pip 失败 → 应该立即检查其他 Python 版本

**改进**：
```python
# 安装失败后的检查清单（新增）

if pip install fails:
    ✅ 检查是否有其他 Python 版本（python3.10, python3.11, etc.）
    ✅ 检查 uv 环境（uv python list）
    ✅ 检查 conda 环境（conda info --envs）
    ✅ 搜索记忆：历史上是否装过？
    ✅ 汇报 DL："失败了，但我验证了 ABC，结论是 XXX"
```

---

### 问题 3：过于依赖"当前会话上下文"

**问题**：我只记得当前会话的事，忘了昨天（跨会话）的事

**根因**：
- 虽然读取了 memory 文件
- 但**没有主动搜索**，只是"被动读取"
- 等 DL 提醒才"恍然大悟"

**改进**：
- ✅ 关键任务前**强制 memory_search**
- ✅ 搜索关键词自动生成（从任务描述提取）
- ✅ 搜索结果必须汇报（"我查了昨天记录，发现 XXX"）

---

## 改进措施（已执行）

### 1. 更新 TODO.md
- [x] 增加"历史检查"项
- [x] 会话启动强制 memory_search

### 2. 更新经验教训索引
- [x] 新增教训 011：失败后多源验证
- [x] 新增教训 012：关键任务前 memory_search

### 3. 更新 MEMORY.md
- [x] 记录本次错误
- [x] 记录改进措施

### 4. 创建复盘文件
- [x] `memory/2026-03-22-Python 版本检查复盘.md`

---

## 核心教训

> **不要只检查"默认"，要搜索"历史"**
> 
> **失败后不要立即汇报"不行"，而是"我验证了 ABC，结论是 XXX"**

---

## 验证方法

**下次会话 DL 可以问**：
1. "安装 widemem-ai 前应该检查什么？"
   - 我应该回答："先 memory_search 搜索历史安装记录"
2. "pip 失败后应该做什么？"
   - 我应该回答："多源验证（检查其他 Python 版本/uv/conda 等）"

---

**复盘时间**: 2026-03-22 10:42  
**责任人**: 小爪  
**监督**: DL
