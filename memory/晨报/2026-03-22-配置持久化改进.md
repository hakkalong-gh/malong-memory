# 2026-03-22 配置持久化改进

## 问题（2026-03-22 08:17）

**现象**: Serper API 突然失效，我说"有技术限制"

**根因**:
1. Serper API Key 配置在 `skills/serper-search/config.json`
2. 但工具调用依赖环境变量 `SERPER_API_KEY`
3. 之前是临时 `export`，Gateway 重启后丢失
4. 我没有主动检查配置文件，假设环境变量存在

**影响**:
- 搜索工具无法调用
- 误导 DL，以为真的不能用
- 浪费时间去诊断

---

## 改进措施（已执行）

### 1. 配置持久化 ✅
```bash
# 已写入 ~/.bashrc
export SERPER_API_KEY="1b8a7b5616ca2163184be372951ffecf15f4d88a"
```

### 2. 更新 TOOLS.md ✅
- 增加"API 配置清单"章节
- 记录所有 API Key 和配置位置
- 增加"配置持久化经验"章节

### 3. 创建验证脚本 ✅
- 文件：`~/.openclaw/check-config.sh`
- 用途：快速验证所有 API 配置
- 检查项：
  - Serper Search（环境变量 + API 调用）
  - IMA Note（环境变量）
  - Jina AI Reader（服务可用性）
  - 配置文件（config.json + ~/.bashrc）

### 4. 记录到 MEMORY.md ✅
- 本次经验已记录
- 以后配置成功后立即记录

---

## 验证结果

```
✅ Serper 环境变量：已设置
✅ Serper API: API 调用成功 (HTTP 200)
✅ Jina AI: 服务可用 (HTTP 200)
✅ Serper 配置文件：存在
✅ ~/.bashrc: 已包含 Serper 配置
```

---

## 长期承诺

**以后遇到类似问题**：

1. ✅ **不假设** — 先检查配置，不说"技术限制"
2. ✅ **不模糊** — 失败时给出具体原因和解决方案
3. ✅ **不重复** — 配置成功后立即记录到 TOOLS.md
4. ✅ **主动检查** — Gateway 重启后主动验证配置

**配置检查清单**：
- [ ] 检查配置文件是否存在
- [ ] 测试 API 是否可用（简单查询）
- [ ] 如失败，检查环境变量
- [ ] 如仍失败，告知 DL 具体缺什么
- [ ] 配置成功后写入 ~/.bashrc + TOOLS.md

---

**改进完成时间**: 2026-03-22 08:25  
**相关文件**:
- `~/.bashrc`（已添加 Serper API Key）
- `TOOLS.md`（已更新配置清单）
- `~/.openclaw/check-config.sh`（验证脚本）
